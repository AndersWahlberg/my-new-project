name: ethico
description: "Ethico mobile client: understand the companies behind products."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  http: 1.3.0
  mobile_scanner: 6.0.2
  url_launcher: 6.3.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
